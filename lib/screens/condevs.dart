import 'package:flutter/material.dart';

class ConDevsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Connected Devices'),
      ),
      body: Center(
        child: const Text('Hello'),
      ),
    );
  }
}